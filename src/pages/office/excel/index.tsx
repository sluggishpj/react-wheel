import { Button, Card, DatePicker, Input, Form, InputNumber, Radio, Select, Tooltip } from 'antd';
import React, { Component } from 'react';

class ExcelTools extends Component {
  render() {
    return <h2>Excel</h2>;
  }
}
export default ExcelTools;
